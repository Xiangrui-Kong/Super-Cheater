#include <stdio.h>
//定义main函数
int main()
{
    //请在此添加‘输入一个字符，按规定格式输出该字符ASCII码’的代码
    /*****************Begin******************/

    char a;
    a = getchar();
    printf("\"The ASCII of '%c' is %d\".",a,a);
    /***************** End ******************/
    return 0;
}
